name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

# Éviter les déploiements concurrents
concurrency:
  group: "pages"
  cancel-in-progress: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  # Job de build
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'yarn'

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build application
        run: yarn build
        env:
          # Base path pour GitHub Pages (dépôt de projet)
          # Pour un dépôt de projet : /nom-du-repo/
          # Pour un dépôt user/organization à la racine : /
          BASE_PATH: '/lidar-viewer/'

      - name: Configure data URL for Cloudflare R2 Worker
        run: |
          # Configurer l'URL de base pour pointer vers le Cloudflare Worker (proxy R2)
          cat > dist/data-config.json << 'EOF'
          {
            "dataBaseUrl": "https://r2-proxy-worker.datawrap.workers.dev",
            "description": "Configuration pour les fichiers de données LiDAR hébergés sur Cloudflare R2 via Worker"
          }
          EOF
          
          # Copier laz-perf.wasm (nécessaire pour la décompression LAZ)
          if [ -f "public/laz-perf.wasm" ]; then
            cp public/laz-perf.wasm dist/ 2>/dev/null || true
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  # Job de déploiement (séparé du build)
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

